- @conversations.each do |conversation|
  - if conversation.participants != @user
    %ul
      %li
        -#= conversation.participants
        = link_to conversation.subject, conversation
        = conversation.updated_at.strftime("%a, %m/%e/%Y %I:%M %p")
        %p
          = link_to "Move to Trash", {:controller => "conversations", :action => "untrash", :id => conversation.id},  :title => "Move to Inbox", :method=>'post'
        %br
        %br