%h2 Notes
%table{:class => "table table-hover"}
  %thead
    %tr
      %th Description
      %th Pomodoro
  %tbody
    - @records.each do |record|
      %tr
        %td= record.description
        %td= record.pomodoro_id

= link_to 'New record', new_record_path
