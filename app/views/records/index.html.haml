
%table
  %thead "Break down by Dates"
  %tr
    %th Date
    %th Description
    %th Encounter
    %th Modify
    %th Delete


  - @records.group_by{ |m| m.created_at.strftime("%Y-%d-%m") }.each do |date, record|
    %tr
      %td{:rowspan => record.size + 1  }
        = date
      %td
        =record.first.description
      %td
        =record.first.encounter_id
      %td
        =link_to " Modify ", edit_record_path(record.first.id)
      %td
        = button_to " x ", {:controller => :records,
                             :action => 'destroy', :id => record.first.id }, :method => :delete
    -record.each do |t|

      %tr
        %td
          =t.description
        %td
          =t.encounter_id
        %td
          =link_to " Modify ",  edit_record_path(t.id)
        %td
          = button_to " x ", {:controller => :records, :action => 'destroy', :id => t.id }, :method => :delete


= link_to 'New record', new_record_path